# 监听端口
port 6377

# 绑定地址（只允许本机访问）
bind 127.0.0.1

# 密码（可选）
# requirepass yourpassword

# 后台运行（Windows 下忽略）
daemonize no

# 数据持久化：RDB 快照
save 900 1
save 300 10
save 60 10000

# 日志文件（Windows 下通常忽略）
logfile "redis.log"

# 数据目录
dir data

